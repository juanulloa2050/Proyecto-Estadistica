---
title: "Proyecto"
author: "Sebastian Ulloa, Juliana Amezquita, Juan David Hoyos, Laura Yepes"
date: "2024-02-19"
output: html_document
---

```{r}
library(readxl)
library(dplyr)
library(cowplot)
Base_de_Datos_Encuesta_IA <- read_excel("Base de Datos Encuesta IA.xlsx")
View(Base_de_Datos_Encuesta_IA)
```

```{r}
summary(Base_de_Datos_Encuesta_IA)
```

<<<<<<< HEAD
```{r}
pastel <- Base_de_Datos_Encuesta_IA %>% 
  count(carrera) %>% 
  mutate(pcnt = n / sum(n),
         etiquetas = scales::percent(pcnt),
         midangle = cumsum(pcnt) - pcnt / 2)

colores <- c("#B22222","#FF3030","#EE2C2C","#CD2626","#8B1A1A","red3")

pastel1 <- ggplot(pastel, aes(x = 1, y = pcnt, fill = carrera)) +
  geom_bar(stat = "identity", color = "black") +
  geom_text(aes(x = 1.2, y = pcnt, label = etiquetas),
            position = position_stack(vjust = 0.5),
            color = "white", size = 4,
            angle = pastel$midangle) +
  guides(fill = guide_legend(title = "Categorías")) +
  scale_fill_manual(values = colores) +
  coord_polar(theta = "y") + 
  theme_void() +
  theme(legend.position = "right") +
  ggtitle("Carreras")
pastel1

bar <- ggplot(Base_de_Datos_Encuesta_IA, aes(x = carrera )) +
  geom_bar(stat="count",fill="#8B1A1A",color='black')+
  cowplot::theme_cowplot()+
  geom_text(aes(label=..count..), stat='count', 
            position=position_dodge(0.9), 
            vjust=-0.5, 
            size=3.0)+
  ylab("Frecuencia") +
  xlab("Uso de gafas")+
  ggtitle("Número de estudiantes por respuesta sobre uso de gafas")
bar
```
```{r}
pastel <- Base_de_Datos_Encuesta_IA %>% 
  count(A) %>% 
  mutate(pcnt = n / sum(n),
         etiquetas = scales::percent(pcnt),
         midangle = cumsum(pcnt) - pcnt / 2)

colores <- c("#B22222","#FF3030","#EE2C2C","#CD2626","#8B1A1A","red3")

pastel1 <- ggplot(pastel, aes(x = 1, y = pcnt, fill = A)) +
  geom_bar(stat = "identity", color = "black") +
  geom_text(aes(x = 1.2, y = pcnt, label = etiquetas),
            position = position_stack(vjust = 0.5),
            color = "white", size = 4,
            angle = pastel$midangle) +
  guides(fill = guide_legend(title = "Categorías")) +
  scale_fill_manual(values = colores) +
  coord_polar(theta = "y") + 
  theme_void() +
  theme(legend.position = "right") +
  ggtitle("¿Qué tan familiarizado estas con la inteligencia artificial?")
pastel1
```


```{r}

# Paso 3: Crear el gráfico de barras
ggplot(Base_de_Datos_Encuesta_IA, aes(x = B)) +  # Especifica la columna que deseas en el eje x
  geom_bar()
>>>>>>> 023a61b0f7ffe8b85b2a43a5ca5ab2abf25e9929
```

```{r}
library(ggplot2)

# Paso 3: Crear el gráfico de barras
ggplot(Base_de_Datos_Encuesta_IA, aes(x = C)) +  # Especifica la columna que deseas en el eje x
  geom_bar()
```

```{r}
# Paso 3: Crear el gráfico de barras
ggplot(Base_de_Datos_Encuesta_IA, aes(x = D)) +  # Especifica la columna que deseas en el eje x
  geom_bar()
```

**PREGUNTA 6**

Estadisticos resultantes a partir de las respuestas:

```{r,warning = FALSE, message = FALSE}
summary(Base_de_Datos_Encuesta_IA$F)
```

Tabla de Frecuencias Absolutas:

```{r}
frec.abs6<-table(Base_de_Datos_Encuesta_IA$F)
frec.abs6
```

Tabla de Frecuencias relativas Porcentuales:

```{r}
n6 <- sum(frec.abs6)
frec.rel6<-round(frec.abs6/n6,2)*100
frec.rel6
```

```{r}
ggplot(Base_de_Datos_Encuesta_IA, aes(x = F)) + 
  geom_histogram(color = "black", fill = "blue", 
                 bins = 6)+
  xlab("Horas a la Semana")+
  ylab("Frecuencia")+
  ggtitle("Histograma")
```

**Conclusion:** Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.

***PREGUNTA 7***

Estadisticos resultantes a partir de la base de datos:
```{r,warning = FALSE, message = FALSE}
summary(Base_de_Datos_Encuesta_IA$G)
```

Tabla de Frecuencias Absolutas:

```{r}
frec.abs7<-table(Base_de_Datos_Encuesta_IA$G)
frec.abs7
```

Tabla de Frecuencias relativas Porcentuales:

```{r}
n7 <- sum(frec.abs6)
n7
frec.rel7<-round(frec.abs7/n7,2)*100
frec.rel7
```

```{r}
ggplot(Base_de_Datos_Encuesta_IA, aes(x = G)) + 
  geom_histogram(color = "black", fill = "pink", 
                 bins = 6)+
  xlab("Dinero en COP")+
  ylab("Frecuencia")+
  ggtitle("Dinero Gastado en dispossitivos con IA")
```

**Conclusion:** Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.

***PREGUNTA 8***

Estadisticos resultantes a partir de la base de datos:
```{r,warning = FALSE, message = FALSE}
summary(Base_de_Datos_Encuesta_IA$H)
```

Tabla de Frecuencias Absolutas:

```{r}
frec.abs7<-table(Base_de_Datos_Encuesta_IA$H)
frec.abs7
```

Tabla de Frecuencias relativas Porcentuales:

```{r}
n7 <- sum(frec.abs6)
n7
frec.rel7<-round(frec.abs7/n7,2)*100
frec.rel7
```

```{r}
ggplot(Base_de_Datos_Encuesta_IA, aes(x = H)) + 
  geom_bar(color = "black", fill = "gray", 
                )+
  xlab("Horas a la Semana")+
  ylab("Frecuencia")+
  ggtitle("Dinero Gastaso en (COP)")
```